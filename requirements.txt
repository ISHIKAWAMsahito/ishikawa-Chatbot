# --- Web & API ---
fastapi==0.115.12
pydantic==2.11.9
python-multipart==0.0.20
uvicorn==0.34.2
requests==2.32.5
httpx>=0.27.0        # バージョンを明示推奨
h11>=0.16.0          # 【追加】CWE-444 (HTTP Request Smuggling) 修正
anyio>=4.4.0         # 【追加】CWE-362 (Race Condition) 修正
zipp>=3.19.1         # 【追加】CWE-835 (Infinite loop) 修正
beautifulsoup4

# --- 認証 & セキュリティ ---
Authlib==1.6.3
itsdangerous==2.2.0
certifi==2025.10.5

# --- Google Gemini（チャット・Embedding・PDF高精度OCRの Vision 兼用）---
google-generativeai==0.8.5

# --- Supabase DB ---
supabase==2.24.0

# --- ドキュメント処理（標準抽出 + PDF画像化OCR）---
# PyPDF2==3.0.1      # 【削除】脆弱性あり・開発終了のため削除
pypdf>=6.4.0         # PDF テキスト抽出（標準モード）
python-docx==1.2.0   # Word 抽出
langchain-text-splitters
# PDF→画像（高精度OCRモード）。Render では render.yaml の buildCommand で poppler-utils 導入（Dockerfile でも可）
pdf2image>=1.17.0
Pillow>=10.0.0       # 画像処理（JPEG 化・Vision 入力）

# --- 監視・トレース・リトライ (★重要: LangSmith対応) ---
langsmith
tenacity
python-dotenv==1.1.0
prometheus-fastapi-instrumentator==7.1.0

async-lru

sqlalchemy
psycopg2-binary